{
  plugins.snacks = {
    enable = true;
    settings = {
      # notify.enable = true;
      terminal.enable = true;
      # notifier = {
      #   enable = true;
      #   style = "fancy";
      # };
    };
  };
}
